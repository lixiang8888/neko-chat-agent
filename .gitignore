# ---------------------------------------------------------------------------
# 密钥 —— 永远不要提交
# ---------------------------------------------------------------------------
# 历史上这个工程的 key 曾经以明文躺在 readme.md 里，而 readme.md 没有被忽略。
# 所以这里宁可写宽一点：任何看起来像密钥文件的都挡掉。
keys.py
*.key
.env
.env.*
secrets.*
*credentials*

# ---------------------------------------------------------------------------
# 运行时产物
# ---------------------------------------------------------------------------
saves/
*.sealed
*.broken-*.json

# ---------------------------------------------------------------------------
# 截图（14MB 二进制，不进历史；要发图请用 GitHub 网页端拖拽上传）
# ---------------------------------------------------------------------------
docs/shots/

# ---------------------------------------------------------------------------
# 本地备份
# ---------------------------------------------------------------------------
# README.original.md 是删改前的原文，只留本地做对照，不入库。
README.original.md

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.uv/

# ---------------------------------------------------------------------------
# 编辑器 / 系统
# ---------------------------------------------------------------------------
.DS_Store
.idea/
.vscode/
.plan_solve/
